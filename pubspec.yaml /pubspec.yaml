name: vocab_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_tts: ^3.8.5
  shared_preferences: ^2.2.2

flutter:
  uses-material-design: true
  assets:
    - assets/beginner.json
    - assets/intermediate.json
    - assets/advanced.json
